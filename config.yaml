FILES:
  TRAIN:
    PATH: "data/train.csv"
  TEST:
    PATH: "data/test.csv"


model_params:
  learning_rate: 0.01
  n_estimators: 300
  max_depth: 5
  loss: "absolute_error"

categorical_columns:
  - "type"
  - "sector"

target_column: "price"